#include "forward_reachability.h"

namespace aaltitoad {
    auto forward_reachability_searcher::is_reachable(const aaltitoad::ntta_t &model, const ctl::query &q) -> bool {

        return false;
    }
}
