#include "ctl.h"

namespace aaltitoad::ctl {
    auto is_satisfied(const query& q, const ntta_t& state) -> bool {
        // TODO: queries should be their own grammar - expr cannot accept location names
        return false;
    }
}
